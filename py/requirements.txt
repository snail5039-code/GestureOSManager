# GestureOSManager agent requirements (Windows / Python 3.10)
# Note: Do NOT install tensorflow in this environment (protobuf conflict).

# ---- Core numeric ----
numpy==1.26.4

# ---- Vision ----
opencv-python==4.9.0.80
mediapipe==0.10.21

# ---- Input automation ----
pyautogui==0.9.54
pillow>=10.0.0
pygetwindow==0.0.9
pyrect==0.2.0
pyscreeze==1.0.1
pytweening==1.2.0
mouseinfo==0.1.3
pymsgbox==2.0.1
pyperclip==1.11.0

# ---- WebSocket client ----
websocket-client==1.7.0

# ---- Compatibility pin (avoid TF/protobuf conflict) ----
protobuf<5

PySide6==6.6.3
requests==2.31.0